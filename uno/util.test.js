const { isNextPlayer, getNextPlayer, getCurrentPlayer } = require('./util')
let state

beforeEach(() =>{
    state = {
        players: {
            1:{
                
            }
        }
    }
})